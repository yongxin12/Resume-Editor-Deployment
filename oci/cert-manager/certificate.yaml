apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: resume-app
spec:
  dnsNames:
    - resume.mintmelon.ca
  secretName: resume-app-tls
  issuerRef:
    name: letsencrypt-cluster-issuer
    kind: ClusterIssuer