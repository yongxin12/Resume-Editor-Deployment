apiVersion: apps/v1
kind: Deployment
metadata:
  name: market-backend
  namespace: resume-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: market-backend
  template:
    metadata:
      labels:
        app: market-backend
    spec:
      containers:
        - name: market-backend
          image: yyz.ocir.io/yztdqx0npeqw/market-backend:latest
          ports:
            - containerPort: 5001
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: db-secrets
                  key: database-url
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          # Using the root path as a readiness check since /health doesn't exist
          readinessProbe:
            httpGet:
              path: /
              port: 5001
            initialDelaySeconds: 10
            periodSeconds: 30
          # Using the root path as a liveness check since /health doesn't exist
          livenessProbe:
            httpGet:
              path: /
              port: 5001
            initialDelaySeconds: 20
            periodSeconds: 60
---
apiVersion: v1
kind: Service
metadata:
  name: market-backend-service
  namespace: resume-app
  annotations:
    service.beta.kubernetes.io/oci-load-balancer-shape: "10Mbps"
spec:
  selector:
    app: market-backend
  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001
  type: ClusterIP 
